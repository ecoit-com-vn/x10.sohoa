apiVersion: apps/v1
kind: Deployment
metadata:
  name: sohoa-frontend
  namespace: x10
  labels:
    app: sohoa-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sohoa-frontend
  template:
    metadata:
      labels:
        app: sohoa-frontend
    spec:
      containers:
      - name: frontend
        image: harbor.ecoit.vn/x10/sohoa.frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4000
          name: http-port
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "4000"
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 4000
          initialDelaySeconds: 30
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /
            port: 4000
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: sohoa-frontend-svc
  namespace: x10
  labels:
    app: sohoa-frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 4000
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: sohoa-frontend
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sohoa-frontend-ingress
  namespace: x10
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  ingressClassName: nginx
  rules:
  - host: qlshx10.ecoit.com.vn
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: apigateway
            port:
              number: 8080
      - path: /hubs
        pathType: Prefix
        backend:
          service:
            name: apigateway
            port:
              number: 8080
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sohoa-frontend-svc
            port:
              number: 80
