-- Script0001_Init_Identity_Tables.sql

CREATE TABLE ORGANIZATION_UNIT (
    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Code VARCHAR2(50) NOT NULL,
    Name VARCHAR2(255) NOT NULL,
    ParentId NUMBER NULL,
    Description VARCHAR2(1000) NULL,
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CreatedBy VARCHAR2(50),
    UpdatedAt TIMESTAMP,
    UpdatedBy VARCHAR2(50)
);

CREATE TABLE APP_USER (
    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    UserName VARCHAR2(50) NOT NULL UNIQUE,
    Email VARCHAR2(100) NULL,
    FullName VARCHAR2(255) NOT NULL,
    PasswordHash VARCHAR2(255) NOT NULL,
    IsActive NUMBER(1) DEFAULT 1,
    OrganizationUnitId NUMBER NULL,
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CreatedBy VARCHAR2(50),
    UpdatedAt TIMESTAMP,
    UpdatedBy VARCHAR2(50),
    CONSTRAINT fk_appuser_orgunit FOREIGN KEY (OrganizationUnitId) REFERENCES ORGANIZATION_UNIT(Id)
);

CREATE TABLE ROLE (
    Id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    Code VARCHAR2(50) NOT NULL UNIQUE,
    Name VARCHAR2(255) NOT NULL,
    Description VARCHAR2(1000) NULL,
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CreatedBy VARCHAR2(50),
    UpdatedAt TIMESTAMP,
    UpdatedBy VARCHAR2(50)
);

CREATE TABLE USER_ROLE (
    UserId NUMBER NOT NULL,
    RoleId NUMBER NOT NULL,
    PRIMARY KEY (UserId, RoleId),
    CONSTRAINT fk_userrole_user FOREIGN KEY (UserId) REFERENCES APP_USER(Id),
    CONSTRAINT fk_userrole_role FOREIGN KEY (RoleId) REFERENCES ROLE(Id)
);
